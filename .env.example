# Copy to .env (which is gitignored). DO NOT commit .env.
#
# Get this URL by opening the "Latin III Cumulative Vocabulary List" sheet,
# then File > Share > Publish to web > select the right tab > "Comma-separated
# values (.csv)" > Publish. Copy the resulting long URL.
CSV_URL=https://docs.google.com/spreadsheets/d/e/REPLACE-ME/pub?output=csv
